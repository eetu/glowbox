{
	"extends": "../../tsconfig.base.json",
	"compilerOptions": {
		// Resolve the sibling core from source so typecheck/dev need no prior build.
		// (The published dep is `@glowbox/core` via package.json `dependencies`.)
		"paths": {
			"@glowbox/core": ["../core/src/index.ts"]
		}
	},
	"include": ["src"]
}
